name: Premier Workflow

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Afficher un message
        run: echo "Bonjour Github Actions"
  
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Afficher le contenu du fichier html
        run: cat index.html